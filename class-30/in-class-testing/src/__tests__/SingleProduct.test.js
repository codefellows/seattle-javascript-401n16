import React from 'react';
import { render } from 'enzyme';
import SingleProduct from '../components/SingleProduct';

//const component = render(<SingleProduct />);

describe('', () => {
    xit('', () => {
        expect(true).toBe(true);
    });
});
